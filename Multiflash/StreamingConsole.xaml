<UserControl x:Class="JBlam.Multiflash.StreamingConsole"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JBlam.Multiflash"
             xmlns:helpers="clr-namespace:JBlam.Multiflash.Helpers"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:StreamingConsoleViewModel}"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="BlanchedAlmond">
    <Label DockPanel.Dock="Bottom" FontFamily="Consolas">
        <ItemsControl ItemsSource="{Binding Output}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Data}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Label>
</UserControl>
